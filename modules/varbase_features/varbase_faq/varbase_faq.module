<?php 

function varbase_faq_page_attachments(array &$page) {
  $current_path = \Drupal::service('path.current')->getPath();
  if (substr($current_path, 1 , 3)== 'faq') {
    $page['#attached']['library'][] = 'varbase_faq/faq-styles';
  }
}